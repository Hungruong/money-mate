# Server Configuration
server.port=8083

# PostgreSQL (AWS RDS)
spring.datasource.url=jdbc:postgresql://money-mate-db.cpgs2swwy3f4.us-east-2.rds.amazonaws.com:5432/money_mate
spring.datasource.username=admin_user
spring.datasource.password=moneymate123
spring.datasource.driver-class-name=org.postgresql.Driver

# JPA & Hibernate Configuration
spring.jpa.database-platform=org.hibernate.dialect.PostgreSQLDialect
spring.jpa.hibernate.ddl-auto=update
spring.jpa.properties.hibernate.show_sql=true
spring.jpa.properties.hibernate.format_sql=true
spring.jpa.open-in-view=false

# AWS S3 Configuration
aws.s3.bucket-name=money-mate-receipts
aws.s3.region=us-east-2

# AWS DynamoDB Configuration
aws.dynamodb.endpoint=https://dynamodb.us-east-2.amazonaws.com
aws.dynamodb.region=us-east-2
aws.dynamodb.table=scanned_bills

# AWS Credentials - N?u s? d?ng AWS CLI thì không c?n thi?t (dùng IAM Role ho?c ~/.aws/credentials)
#aws.accessKeyId=YOUR_ACCESS_KEY
#aws.secretAccessKey=YOUR_SECRET_KEY

# Logging Configuration (Tùy ch?n, giúp debug l?i khi k?t n?i AWS)
logging.level.org.hibernate.SQL=DEBUG
logging.level.org.springframework.web=INFO
logging.level.com.amazonaws.util.EC2MetadataUtils=ERROR
